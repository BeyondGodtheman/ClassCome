<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="158pt"
                android:layout_marginBottom="42pt"
                android:background="@drawable/draw_bg_fillet_white">

    <TextView
        android:id="@+id/txt_type"
        style="@style/style_font_default_36pt"
        android:layout_marginStart="56pt"
        android:layout_marginTop="28pt"
        android:textStyle="bold"
        tools:text="@string/publish_train"/>

    <TextView
        android:id="@+id/txt_info"
        style="@style/style_font_gray_24pt"
        android:layout_below="@id/txt_type"
        android:layout_alignStart="@id/txt_type"
        android:layout_marginTop="22pt"
        tools:text="@string/publish_train_prompt"/>

    <ImageView
        android:id="@+id/img_icon"
        android:layout_width="84pt"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="64pt"
        android:contentDescription="@string/app"
        tools:src="@mipmap/ic_publish_tutor"/>

</RelativeLayout>
